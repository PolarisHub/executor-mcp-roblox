/** Returns the full dashboard HTML page (self-contained: inlined CSS + JS). */
export function renderDashboardPage(): string {
  return "<!doctype html><title>executor-mcp-roblox</title><h1>dashboard</h1>";
}
